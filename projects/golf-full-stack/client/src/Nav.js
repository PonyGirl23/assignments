import React from 'react'

function Nav() {
    return (
        <div className="Brand">
        <h1>Round Tracker</h1>
        <h1>Save and compare data on every course, every time...</h1>
        </div>
    )
}

export default Nav